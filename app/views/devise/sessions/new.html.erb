  <div class="container">
    <div class="row">
      <div class="col-md-4 col-md-offset-4">
        <div class="panel panel-default">
          <div class="panel-heading">
            <h3 class="panel-title">Sign in. To see it in action.</h3>
          </div>
          <div class="panel-body">
            <%= devise_error_messages! %>

            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
              <fieldset>
                <div class="form-group">
                  <%= f.text_field :login, autofocus: true, :class => "form-control", :placeholder => "Username or Email" %>
                </div>

                <div class="form-group">
                  <%= f.password_field :password, autocomplete: "off", :class => "form-control", :placeholder => "Password" %>
                </div>

                <% if devise_mapping.rememberable? -%>
                  <div class="form-group checkbox">
                    <label>
                      <%= f.check_box :remember_me %>
                      <%= f.label :remember_me %>
                    </label>
                  </div>
                <% end -%>

                <div class="form-group actions">
                  <%= f.submit "Sign in", :class => "btn btn-primary btn-block" %>
                </div>
              </fieldset>
            <% end %>

            <p class="text-muted text-center">
            <%= link_to('<small>'.html_safe + 'Forgot password?' + '</small>'.html_safe, new_user_password_path)  %>
            </p>
            <p class="text-muted text-center"><medium>Do not have an account?</medium></p>
            <%= link_to('Sign up', new_user_registration_path, { :class => "btn btn-default btn-block" })  %>
            <br>
          </div>
        </div>
      </div>
    </div>
  </div>
